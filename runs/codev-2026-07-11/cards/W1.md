**Task:** Repoint the default model of the 100-prompt regression harness from the dethroned quant to the current champion. Two-line, harness-only change; no behavior change to the product.

Numbered steps — follow exactly:

1. Open `crates/claudette/tests/claudette100_test.sh`.
2. On line 14, replace the doc-comment line:
   ```
   #   Defaults: model=$CLAUDETTE_MODEL or qwen3.6-35b-a3b@q4_k_xl
   ```
   with:
   ```
   #   Defaults: model=$CLAUDETTE_MODEL or qwen3.6-35b-a3b-mtp
   #   (byteshape champion; expects LM Studio loaded per
   #    runs/eval-2026-05-29/battery/champion-launch.md)
   ```
3. On line 19, replace:
   ```
   MODEL="${1:-${CLAUDETTE_MODEL:-qwen3.6-35b-a3b@q4_k_xl}}"
   ```
   with:
   ```
   MODEL="${1:-${CLAUDETTE_MODEL:-qwen3.6-35b-a3b-mtp}}"
   ```
4. Touch NOTHING else in this file — no reformatting, no other lines.

**Do NOT touch:**
- `runs/eval-2026-05-29/battery/MODEL-COMPARISON.md`
- any file under `tests/results_*`
- any `tests/*_prompts.txt`
- any file not named above (this task touches exactly ONE file).

**Gate (run before opening the PR):** `cargo fmt --all && cargo clippy --all-targets --all-features --no-deps -- -D warnings && cargo test && cargo test --all-features` — all must pass.

**Commit message (exactly, no Co-Authored-By trailer, no other trailers):**
```
test(claudette100): default harness model to the byteshape champion

The 100-prompt harness still defaulted to qwen3.6-35b-a3b@q4_k_xl, which
was dethroned 2026-07-11. Point the fallback at qwen3.6-35b-a3b-mtp (the
LM Studio identifier of the byteshape MTP-GPU-2 champion) and note the
expected launch config in the usage comment.
```

**Expected diff:** 1 file (`crates/claudette/tests/claudette100_test.sh`), ~4 lines changed (2 replaced, 2 added comment lines).

Open the PR (title: `test(claudette100): default harness model to the byteshape champion`); **STOP.**
