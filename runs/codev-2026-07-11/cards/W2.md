**Task:** Refresh the stale `--doctor` brain-recommendation table in `crates/claudette/src/hw.rs` from the 2026-07 battery results (score/id/text refresh ONLY — do not change any threshold, function signature, or control flow).

Numbered steps — follow exactly:

1. Open `crates/claudette/src/hw.rs`. All edits are in this ONE file.

2. Replace the doc-comment block starting `/// Map (VRAM GiB, backend) → the certified brain for that tier.` and ending `/// selection stays with \`brain_selector\`.` (currently ~lines 83–94) with exactly:
   ```rust
   /// Map (VRAM GiB, backend) → the certified brain for that tier.
   ///
   /// Seeded from the README tier table (50-task battery: v0.16.0 sweep
   /// 2026-07-10 + champion-tuning campaign 2026-07-11) and **backend-honest**:
   /// the 50/50 champion `byteshape/qwen3.6-35b-a3b-mtp` is distributed via
   /// LM Studio, NOT packaged on Ollama — recommending it for `ollama pull`
   /// would fail, so on an Ollama backend we recommend the best pullable
   /// brain and point at the backend switch. `qwen3.5:4b` (45/50 + K 8/8 on
   /// ~3.4 GB) is that pick for BOTH Ollama tiers: the previous 9b pick
   /// mis-renders its chat template on current runtimes (empty turns) and
   /// gpt-oss-20b trails at 41/50. Advisory only — runtime model selection
   /// stays with `brain_selector`.
   ```
   Do NOT touch the `/// The flagship tier line.` paragraph below it or the `FLAGSHIP_TIER_GIB` const.

3. In `recommend_brain`, replace the first `BrainRec` literal (the `openai_compat` = true arm, currently `model: "qwen3.6-35b-a3b@q3_k_xl"`) with exactly:
   ```rust
   BrainRec {
       model: "byteshape/qwen3.6-35b-a3b-mtp",
       why: "50/50 + K 8/8 on the 50-task battery (49/50 at 64k ctx) at \
             ~70-76 tok/s — fully VRAM-resident in 13.6 GB, zero RAM spill. \
             Load with the README's champion command (ctx 65536 + MTP flags)",
       alternatives: "same-score official-lineage qwen3.6-35b-a3b@iq4_xs \
                      (50/50, spills to RAM, 27.8 tok/s); known-good rollback \
                      qwen3.6-35b-a3b@q3_k_xl (47/50, 33.8 tok/s)",
   }
   ```

4. Replace the second `BrainRec` literal (the Ollama flagship arm, currently `model: "qwen3.5:9b"`) with exactly:
   ```rust
   BrainRec {
       model: "qwen3.5:4b",
       why: "45/50 (90%) + K 8/8 on the 50-task battery — the best brain \
             packaged on Ollama; the previous 9b pick mis-renders its chat \
             template on current runtimes (empty turns) and is no longer \
             certified",
       alternatives: "the 50/50 champion byteshape/qwen3.6-35b-a3b-mtp is \
                      LM Studio-only: set CLAUDETTE_OPENAI_COMPAT=1 + \
                      OLLAMA_HOST=http://localhost:1234 + \
                      CLAUDETTE_MODEL=byteshape/qwen3.6-35b-a3b-mtp (full \
                      setup: docs/power-user.md; worth the switch on 16 GB)",
   }
   ```

5. Replace the third `BrainRec` literal (the sub-tier `else` arm) with exactly:
   ```rust
   BrainRec {
       model: if openai_compat {
           "qwen3.5-4b"
       } else {
           "qwen3.5:4b"
       },
       why: "45/50 (90%) + K 8/8 on the 50-task battery from a ~3.4 GB pull — \
             best value; runs on an 8 GB GPU or plain CPU",
       alternatives: "gpt-oss-20b (41/50, ~13 GB, fastest full-battery run) \
                      if you have the headroom",
   }
   ```

6. In the test `recommend_boundaries_match_the_certified_table`:
   - Change the comment line `// Flagship tier + LM Studio → the 35b, quant pinned. 15.9 is what a` to `// Flagship tier + LM Studio → the byteshape champion. 15.9 is what a`.
   - Change the three assertions expecting `"qwen3.6-35b-a3b@q3_k_xl"` (at 15.9 / 16.0 / 24.0) to expect `"byteshape/qwen3.6-35b-a3b-mtp"`.
   - Change `assert_eq!(ollama16.model, "qwen3.5:9b");` to `assert_eq!(ollama16.model, "qwen3.5:4b");`.
   - Leave the `CLAUDETTE_OPENAI_COMPAT=1` contains-assertion and every assertion from `recommend_brain(14.9, false)` down UNCHANGED.

7. Change NOTHING else: no threshold edits, no new functions, no formatting sweeps, no other files.

**Do NOT touch:**
- `runs/eval-2026-05-29/battery/MODEL-COMPARISON.md`
- any file under `tests/results_*`
- any `tests/*_prompts.txt`
- any file not named above (this task touches exactly ONE file: `crates/claudette/src/hw.rs`).

**Gate (run before finishing):** `cargo fmt --all && cargo clippy --all-targets --all-features --no-deps -- -D warnings && cargo test && cargo test --all-features` — all must pass.

**IMPORTANT — do NOT run `git add`, `git commit`, or open a PR yourself.** Leave the changes uncommitted in the working tree; the mission pipeline stages, commits, and opens the PR. Commit message for the pipeline (exactly, no Co-Authored-By trailer):
```
fix(doctor): refresh brain recommendations from the 2026-07 batteries

recommend_brain still cited the 2026-05-30 battery (92%/88%/86%) and
recommended qwen3.5:9b on Ollama, which mis-renders its chat template on
current runtimes. Repoint the LM Studio flagship tier at the byteshape
champion (50/50 + K 8/8, VRAM-resident), the Ollama tier at qwen3.5:4b
(45/50, the best certified pullable), and refresh the sub-16 tier's
alternatives (drop the broken 9b, cite gpt-oss-20b's measured 41/50).
Table/score/id refresh only — thresholds and banding unchanged.
```

**Expected diff:** 1 file (`crates/claudette/src/hw.rs`), roughly −35/+40 lines (three BrainRec literals, one doc block, five test lines).

After the gate passes, STOP — report done and let the pipeline submit.
