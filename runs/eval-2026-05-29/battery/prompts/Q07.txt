The `word_frequencies` function in src/lib.rs is supposed to return words paired with how often they appear, ordered from most frequent to least. Right now the ordering is all over the place and it's fussy about how words are separated and capitalized. Make it robust: treat words case-insensitively, split on any non-alphanumeric characters (punctuation, tabs, newlines — not just spaces), and return the list sorted by count from highest to lowest, breaking ties alphabetically. The existing test should still pass.