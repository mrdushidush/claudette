This crate evaluates simple integer arithmetic expressions (see src/lib.rs, src/lexer.rs, src/eval.rs). There's a bug: chained subtraction comes out wrong. For example "10 - 3 - 2" evaluates to 9, but it should be 5 (subtraction groups left to right: (10 - 3) - 2). Multiplication/division and precedence are fine. Track down the root cause and fix it so expressions with several `+`/`-` operators evaluate correctly. The existing test must still pass.