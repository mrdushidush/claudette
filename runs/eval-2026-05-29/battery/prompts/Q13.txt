The `parse_csv_line` function in solution.py splits a CSV line into fields, but it only handles the simplest case. Make it parse a single line of real CSV properly: a field wrapped in double quotes may contain commas, and inside a quoted field a doubled "" means a literal double-quote character. For example a,"b,c",d has three fields: a, then b,c, then d. The existing test should still pass.