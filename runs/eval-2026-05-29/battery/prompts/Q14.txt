The `deep_merge` function in solution.py is supposed to merge two config dictionaries, with the second one's values winning, and nested dictionaries merged recursively. Right now it has problems: nested dicts get clobbered instead of merged, and it modifies the dictionaries passed in. Fix it so nested dicts merge recursively and neither input dict is mutated. Keep the existing test passing.