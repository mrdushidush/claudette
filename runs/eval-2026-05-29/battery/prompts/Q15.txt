The `retry` helper in solution.py calls a function up to `attempts` times and should return the first successful result. But if every attempt fails, it silently returns None instead of surfacing the error — callers can't tell the difference between "succeeded with None" and "gave up". Fix it so that when all attempts fail it re-raises the exception from the final attempt. It should still return immediately on the first success and not call the function more times than necessary. Keep the existing test passing.