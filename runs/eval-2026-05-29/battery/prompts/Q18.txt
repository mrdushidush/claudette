The `add_item` function in solution.py appends an item to a bucket list and returns it. When called without a bucket it's supposed to start a fresh empty list each time, but instead separate calls are sharing the same hidden list, so items pile up across unrelated calls. Fix it so that each call without an explicit bucket gets its own independent empty list. Keep the existing test passing.