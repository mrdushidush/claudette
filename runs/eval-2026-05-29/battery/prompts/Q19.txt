Finish `human_readable_size` in solution.py. It formats a byte count as a human-friendly string. Below 1024 bytes, show the exact integer count with a " B" suffix (e.g. 0 -> "0 B", 512 -> "512 B"). At 1024 and above, scale up through KB, MB, GB, TB, PB using 1024 as the step between units, and show the value with exactly one decimal place (e.g. 1024 -> "1.0 KB", 1536 -> "1.5 KB", 1048576 -> "1.0 MB"). The current version only ever produces KB and doesn't round. Keep the existing test passing.