The `Counter` class in solution.py is shared across threads, but when several threads call `increment` at the same time the final value comes out too low — updates are being lost because the read and the write aren't done together. Make `increment` safe so that concurrent increments are never lost and the counter ends up equal to the total number of calls. Keep the public interface (`increment`, `get`) the same, and keep the existing test passing.