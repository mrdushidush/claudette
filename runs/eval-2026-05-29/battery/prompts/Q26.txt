Implement `deepEqual(a, b)` in solution.mjs — it should return true when two values are deeply equal. It needs to compare objects and arrays structurally (including nested ones, and the order of object keys shouldn't matter), treat NaN as equal to NaN, and not be fooled by tricky cases like an undefined property versus a missing one, or NaN versus null. The current version leans on JSON.stringify, which gets several of these wrong. You can check the basics with `node test_basic.mjs`; keep that passing.