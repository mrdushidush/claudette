The `sortNumbers` function in solution.mjs should return a new array with the numbers sorted in ascending order. It works for small single-digit arrays but gets multi-digit numbers and negatives wrong (for instance it sorts [10, 2, 1] as [1, 10, 2]). Fix it so it sorts numerically. Don't mutate the input array. You can check the basics with `node test_basic.mjs`; keep it passing.