The `deepClone` function in solution.ts is supposed to return a deep copy of a value, but it only copies one level: nested objects and arrays are still shared with the original, so mutating the copy's nested data also changes the original. Fix it so nested objects and arrays are cloned all the way down and the copy is fully independent of the original. You can check the basics with `node test_basic.ts`; keep it passing.