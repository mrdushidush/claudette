The `range(start, end, step)` function in solution.ts returns numbers from start up to (but not including) end, stepping by step (default 1). It works for ascending ranges but a negative step — which should count down, e.g. range(5, 0, -1) is [5, 4, 3, 2, 1] — just returns an empty array. Fix it so negative steps count downward correctly, while keeping ascending behavior the same. You can check the basics with `node test_basic.ts`; keep it passing.