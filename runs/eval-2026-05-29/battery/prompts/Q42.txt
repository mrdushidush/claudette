Clean up the `isPalindrome` function in solution.ts. It should report whether a string is a palindrome, but ignoring case, spaces, and punctuation — only letters and digits count. So "A man, a plan, a canal: Panama" is a palindrome, and so is "Racecar". Right now it does a raw character-for-character reversal, so those cases come back false. Fix it. You can check the basics with `node test_basic.ts`; keep it passing.