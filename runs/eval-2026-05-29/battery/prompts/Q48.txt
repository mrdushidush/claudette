The script solution.sh joins its command-line arguments together separated by commas. It works when the arguments are single words, but arguments that contain spaces get split apart — for example `bash solution.sh "a b" c` should print "a b,c" but instead prints "a,b,c". Fix it so each argument is treated as a single unit, spaces and all. A single argument should print unchanged with no comma.