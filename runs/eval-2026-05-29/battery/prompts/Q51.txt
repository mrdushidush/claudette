Implement `parse_duration(s)` in solution.py. It turns a duration string into a total number of seconds. Durations are written with the unit suffixes h (hours), m (minutes), and s (seconds), and several units can be combined — for example "1h30m" is 5400 and "90s" is 90. Make it robust for the kinds of strings people actually type. If the input isn't a valid duration, raise a ValueError rather than guessing. The two tests show the basic shape.