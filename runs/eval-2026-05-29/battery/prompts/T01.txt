`normalize_tags` in solution.py turns a comma-separated tag string into a list of tags, but right now it just splits on commas and returns the pieces as-is. Clean it up: each tag should have surrounding whitespace removed and be lowercased, empty tags should be dropped, and duplicate tags should appear only once. The order in which tags first appear must be preserved. Keep the function name and signature the same.
